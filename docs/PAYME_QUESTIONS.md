# Переписка с отделом интеграции Payme

## Ответ специалиста (получен)

- **Частичный холд НЕДОСТУПЕН.** Можно либо заморозить всю сумму, либо сразу провести
  успешный state. Частично списать холд нельзя.
- Холдирование — в методах Subscribe API (отсылка к документации).
- Фискальные данные отправляются методом `receipts.create`; валидатор проверяет их
  при инициализации платёжного метода, затем формируется фискальный признак и чек.
- **IP Payme:** 185.234.113.1 – 185.234.113.15 (прописаны в `PAYME_WEBHOOK_WHITELIST`).
- Тестовые среды Merchant API и Subscribe API **не связаны**, тестируются отдельно.
- **Sandbox Subscribe API на тех. работах** → Subscribe-часть проверять сразу в проде.
- Merchant API тестируется с обеих сторон.
- Для выдачи кредов нужно прислать: endpoint URL, account параметр, почту.

## Наш ответ (готов к отправке)

Добрый день! Спасибо за ответ и за список IP — уже прописали в whitelist.

Данные для выдачи кредов:

- Endpoint URL (Merchant API webhook): https://masteruz.uz/api/payments/webhook/payme
- Account параметр: payment_id
  (в чеке передаётся как account.payment_id — идентификатор платежа в нашей системе)
- Почта для отправки кредов: valdemarius.gl@gmail.com

Просим прислать креды отдельно для sandbox (Merchant API) и для боевой среды
(Merchant API + Subscribe API), раз тестовые среды не связаны.

Уточнения, которые остались открытыми:

1. По холдированию: пришлите, пожалуйста, точное название метода создания холда и
   метода списания/разблокировки холда + пример запроса и ответа. В документации
   хотим свериться именно с вашей актуальной версией, чтобы не ошибиться.
2. Какой максимальный срок жизни холда (через сколько средства авто-разблокируются)?
3. Активированы ли у нашего мерчанта Subscribe API и функция холдирования?
4. Подтвердите ИКПУ 11201001001000000 («Услуги в сфере цифровых информационных
   технологий») для наших операций (комиссия/депозит/доплата).
5. Ставка НДС для этого ИКПУ — 0% или 12%?
6. Нужен ли package_code для услуг, или его можно не передавать?
7. При холдировании чек фискализируется в момент заморозки или в момент финального
   списания? Нужен один чек или два?
8. ИКПУ у нас один (статичный) — подтверждаете, что можно залить на кассу по письму?
   Если да, подскажите, на какой адрес и в каком виде отправить письмо.

Спасибо!
