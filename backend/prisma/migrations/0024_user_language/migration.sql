-- ============================================================================
-- MasterUz — 0024: язык пользователя для локализации уведомлений
-- ----------------------------------------------------------------------------
-- До этой миграции все уведомления (in-app и Telegram) отправлялись только
-- на русском. Теперь язык хранится у пользователя и синхронизируется с
-- выбором в интерфейсе. Значения: 'ru' | 'uz' | 'en'.
-- Существующим пользователям проставляется 'ru' — то, что они видели раньше.
-- ============================================================================

ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "language" TEXT NOT NULL DEFAULT 'ru';
