# MasterUz ignores
node_modules/
dist/
build/

# ── Переменные окружения (всё игнорируем, кроме *.example) ──
.env
.env.*
!.env.example
!.env.production.example
backend/.env
backend/.env.*
!backend/.env.example
frontend/.env
frontend/.env.*
!frontend/.env.example

*.log
uploads/*
!uploads/.gitkeep
backend/data/*.json
!backend/data/.gitkeep
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
coverage/
certbot/
backups/
.vite/

# Local-only files
.venv/
biznes.md
*.pptx
.env.android

# Android build artifacts (Capacitor)
frontend/android/build/
frontend/android/app/build/
frontend/android/.gradle/
frontend/android/local.properties
frontend/android/.idea/
frontend/dist/
frontend/.env.android

# Локальный скрипт с секретами Railway
scripts/railway-set-env.sh

# R2 credentials
.env.r2

# Eval-данные AI: реальные клиентские фото и разметка на их основе.
# В репозиторий не кладём — это персональные данные пользователей.
# Шаблон формата (dataset.example.json) versioned намеренно.
backend/scripts/eval/samples/
backend/scripts/eval/dataset.json
backend/scripts/eval/dataset.draft.json
